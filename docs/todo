/ : partially implemented
// : completed, to be removed.

* PROMPT ENGENEERING:
    - Add system prompt /
    - Add double step query /

* METADATA:
    - generate metadata while other docs are read
    - Implement self query of the vectorstore /

* EVALUATION:
    - Make QNA file. /
    - Evaluate model: compare single/double step queries, chunk sizes, embeddings.
    - Test the different chunk sizes --> test with: {chunksize = 1024, overlap 128}

* CSV/Excel:
    - how to read csv files

* GENERAL:
    - Check time taken by queries. Invoke step takes a while.


